# YT Downloader — Theme + Verification Pattern (session-learned)

Learned: theme = dark default (no system gap), cycle dark↔light, always apply .dark/.light class to <html>; button = dark pill icon-only DARK/LIGHT; Playwright verify nav icon + theme change + download stream + MP3 audio format; user prefers direct automated fixes, luxury-minimal cyan UI.